class ReplaceAll {
import java.util.ReplaceAll;
public static void main(String[] args) {
ArrayList<Integer> list = new ArrayList<>(Arrays.asList(1,2,3,4));
Collections.fill(list, 0);

        System.out.println(list);
    }
}
